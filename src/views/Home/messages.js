import { defineMessages } from 'react-intl';

export const scope = 'app.views.Home';

export default defineMessages({
  LearnReact: {
    id: `${scope}.LearnReact`,
    defaultMessage: 'Learn React',
  },
});
